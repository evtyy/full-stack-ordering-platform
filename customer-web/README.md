# customer-web

React + TypeScript + Vite SPA for browsing the Palette menu and checking out.

See the [repo root README](../README.md) for full setup instructions (backend, database, and how this fits with the admin dashboard).

Quick start:

```
npm install
npm run dev
```
